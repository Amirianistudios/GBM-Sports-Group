{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "echo \"env=$VERCEL_ENV ref=$VERCEL_GIT_COMMIT_REF sha=$VERCEL_GIT_COMMIT_SHA\"; [ \"$VERCEL_GIT_COMMIT_REF\" = main ] && exit 1; [ \"$VERCEL_ENV\" = preview ] && exit 0; exit 1",
  "github": {
    "silent": false
  }
}
